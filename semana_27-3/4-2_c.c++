// ejercicio 4.2 - inciso c
#include <iostream>
#include <math.h>

using namespace std;

int main(int argc, char const *argv[])
{
  int resultado1, resultado2, total;

  //(24 + 2 * 6) / 4
  resultado1 = (2 * 6);
  resultado2 = (24 + resultado1);
  total = (resultado2 / 4);

  cout << "Relresultado es: " << total;
  return 0;
}
